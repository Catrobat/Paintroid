package org.catrobat.paintroid.test.integration;


public class DisableButtonsTest extends BaseIntegrationTestClass {

	public DisableButtonsTest() throws Exception {
		super();
	}

	/*
	 * public void testDisableRedoUndoAfterStart() { assertEquals("Undo Button not disabled", mButtonTopUndo,
	 * (ImageButton) getActivity().findViewById(R.id.btn_status_undo_disabled));
	 * assertEquals("Redo Button not disabled", mButtonTopRedo, (ImageButton)
	 * getActivity().findViewById(R.id.btn_status_redo_disabled));
	 * 
	 * }
	 * 
	 * public void testDisableUndoRedo() { mCurrentDrawingSurfaceBitmap.setPixel(mCurrentDrawingSurfaceBitmap.getWidth()
	 * / 2, mCurrentDrawingSurfaceBitmap.getHeight() / 2, Color.BLACK); // update mButtonTopUndo = (ImageButton)
	 * getActivity().findViewById(R.id.btn_status_undo); mButtonTopRedo = (ImageButton)
	 * getActivity().findViewById(R.drawable.);
	 * 
	 * assertEquals("Undo Button stil disabled", mButtonTopUndo, getActivity().findViewById(R.id.btn_status_undo));
	 * assertEquals("Redo Button not disabled", mButtonTopRedo, (ImageButton)
	 * getActivity().findViewById(R.id.btn_status_redo_disabled));
	 * 
	 * mSolo.clickOnView(mButtonTopUndo);
	 * 
	 * // update mButtonTopUndo = (ImageButton) getActivity().findViewById(R.id.btn_status_undo); mButtonTopRedo =
	 * (ImageButton) getActivity().findViewById(R.id.btn_status_redo);
	 * 
	 * assertEquals("Undo Button not disabled", mButtonTopUndo, (ImageButton)
	 * getActivity().findViewById(R.id.btn_status_undo_disabled)); assertEquals("Redo Button not disabled",
	 * mButtonTopRedo, (ImageButton) getActivity().findViewById(R.id.btn_status_redo_disabled)); }
	 */
}
